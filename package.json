{
  "name": "purescript-dodo-printer",
  "type": "module",
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "format": "purs-tidy format-in-place src test"
  },
  "devDependencies": {
    "purescript": "^0.15.12",
    "purs-tidy": "^0.10.0",
    "spago": "^0.21.0"
  }
}
